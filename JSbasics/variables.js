var myName = "Arohan"; // var can be used anywhere in the program

let num = 8; // a variable that can only be used in the scope of where it was declared

const pi = 3.14; // a variable that can never change

var a; // declaring the variable

var b = 2; // declaring AND assigning the variable